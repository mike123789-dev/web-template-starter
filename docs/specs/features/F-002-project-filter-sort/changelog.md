# Changelog: F-002 Project Filter/Sort

## 2026-02-09

- Initial spec set created (`spec`, `plan`, `tasks`, `test-matrix`)
- Clarification resolved: default sort fixed to `updatedAt desc` with fallback `sort=updated`
- Updated F-002 status to `Ready` and marked T-001 as `Done`
- Expanded AC/test mapping with `AC-004` in `test-matrix.md`
